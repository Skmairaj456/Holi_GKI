{
  "name": "holi-landing",
  "version": "1.0.0",
  "description": "Single-page Holi festival landing page",
  "scripts": {
    "build:css": "tailwindcss -i ./src/styles.css -o ./styles.css --minify",
    "watch:css": "tailwindcss -i ./src/styles.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0",
    "tailwindcss": "^3.0.0"
  }
}
